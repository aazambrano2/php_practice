"use strict";

//return as strings
var num1 = window.prompt("Enter a number");
var num2 = window.prompt("Enter another number"); //parse strings to int

num3 = parseInt(num1) + parseInt(num2); //parseFloat()

document.write(num3);